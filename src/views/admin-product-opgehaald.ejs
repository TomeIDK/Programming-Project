<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" type="text/css" href="./components/button/button.css" />
  <link rel="stylesheet" type="text/css" href="./css/reset.css" />
  <link rel="stylesheet" type="text/css" href="./css/default.css" />
  <link rel="stylesheet" type="text/css" href="./css/opgehaald.css" />
  <link rel="icon" href="/images/favicon.ico" type="image/x-icon" />
  <title>EhB MediaLab - Reservaties opgehaald</title>
</head>

<body>
  <ehb-header></ehb-header>
  <hamburger-menu></hamburger-menu>
  <main>
    <h1>Reservaties opgehaald</h1>

    <% if (reservations && reservations.length > 0) { %>
    <ul class="reservation">
      <li class="reservation__heading">
        <span class="reservation__title"></span>
        <span class="reservation__title">Product</span>
        <span class="reservation__title">username</span>
        <span class="reservation__title">Datum gereserveerd</span>
        <span class="reservation__title">Gereserveerd tot</span>
        <span class="reservation__title">Opgehaald</span>
      </li>
      <% reservations.forEach((reservation, index) => { %>
      <li class="reservation__item reservation__line" data-id="<%= reservation.uitleningID %>">
        <span class="reservation__article--image"></span>
        <span class="reservation__article--name"><%= reservation.productNaam %></span>
        <span class="reservation__article--username"></span>
        <span class="reservation__article--date-from"> <%= reservation.startDatum.toLocaleDateString("nl-NL").split("-").join("/") %></span>
        <span class="reservation__article--date-until"><%= reservation.eindDatum.toLocaleDateString("nl-NL").split("-").join("/") %></span>

        <button id="btn-cancel<%= index %>" class="btn secondary-button cancel-reservation">Opgehaald</button>
      </li>
      <% }) %>
    </ul>
    <% } else { %>
    <p>U heeft nog geen reservaties.</p>
    <% } %>
  </main>
  <ehb-footer></ehb-footer>
  <script src="./js/components.js"></script>
  <script src="./js/reservaties.js"></script>
  <script src="/js/default.js"></script>
</body>

</html>