class Product {
  constructor(id, name, specs, imgUrl, available) {
    this.id = id;
    this.name = name;
    this.specs = specs;
    this.imgUrl = imgUrl;
    this.available = available;
  }
}

module.exports = Product;
